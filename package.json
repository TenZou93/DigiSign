{
  "name": "digisign",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
